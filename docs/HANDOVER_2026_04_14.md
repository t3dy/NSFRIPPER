# Handover: NSFRIPPER session 2026-04-14

## Commits This Session (8+)

| Commit | What |
|--------|------|
| `057db2e` | Hiro Layer 1: expansion audit (297 NSFs, 35 expansion), multi-chip schema, ANTIRIPPER oracle rebuild, token optimization (50% rules reduction) |
| `44a762e` | Batch extraction: ~150 new games, expanded Batman + Ghosts'n'Goblins tracks |
| `b02a7d9` | Layer 2: chip-agnostic Frame IR (`extraction/frame_ir.py`), 38 tests, settings.local.json 33KB->1.5KB |
| `d895584` | Layer 3: expansion audio capture in emulator (FDS+VRC6 verified), Cleopatra 16/16 extracted |
| `930af38` | CLAUDE.md Hiro status, Captain_Tsubasa_II + Cosmic_Wars extractions |
| `f8f835f` | Batch extractor improvements (NSF header fallback, scaled timeouts), Captain_Tsubasa_II 35/41 |

## Hiro Plantagenet Progress

| Layer | Status | Key Output |
|-------|--------|------------|
| 1: Audit & Schema | **Complete** | `data/expansion_audit.json`, `docs/MULTI_CHIP_SCHEMA.md` |
| 2: Frame IR Extensions | **Complete** | `extraction/frame_ir.py` — all 6 chips + DPCM, 38 tests |
| 3: Capture Pipeline | **Partial** | Emulator captures expansion registers (verified FDS+VRC6). Processing/MIDI export TBD |
| 4: Validation Infra | Not started | |
| 5: ROM Parsers | Not started | |
| 6: Synth Fidelity | Not started | |
| 7: Classification | Not started | |

## Extraction Coverage (post-batch)

- 850 game directories in output/
- **321 with MIDI (8936 total MIDIs)** — up from 231 at session start
- 278 with REAPER projects
- 0 NSF-only remaining (all processed or timed out)
- 45 games with partial extraction (<80%) being retried with scaled timeouts
- Driver survey: 181 games classified (F1:68, F2:61, F3:12, F4:36, F5:4)

## Background Tasks (may still be running)

1. **Main batch** (`batch_nsf_all.py`): 7/88 games processed, 3 failures (Cleopatra fixed, Double_Dribble timeout, Exciting_Boxing timeout). Many "unknown tracks" games timeout at 600s. Next batch run should benefit from NSF header fallback + scaled timeouts.

2. **Captain_Tsubasa_II**: 35/41 tracks extracted. Remaining 6 tracks were being extracted individually.

## Bug Fixes

- **Control char in NSF title** (`nsf_to_reaper.py:894`): Cleopatra no Mahou had `\n` at byte 0x2C of title field. Fixed by stripping all control chars (0x00-0x1F) instead of just null bytes.
- **settings.local.json bloat**: 400 one-off permission entries accumulated over sessions. Replaced with ~50 broad wildcard patterns. 96% token reduction.
- **Batch extractor**: "unknown tracks" games now read NSF header byte 6 for total songs. Timeout scales with track count (60s/track, min 600s, max 3600s).

## What Needs to Happen Next

### Immediate
- Commit remaining Captain_Tsubasa_II tracks when extraction finishes
- Commit any new batch extraction output that arrived
- Re-run `batch_nsf_all.py` for the remaining ~80 games (with improved timeouts)

### Layer 3 Completion
- Map expansion register writes to expansion channel data in `frames_to_channel_data()`
- Generate expansion MIDI channels (ch 5-11) per MULTI_CHIP_SCHEMA.md Section 5
- Add DPCM sample detection heuristic (Section 3.3)
- Track $4003/$4007 phase reset writes

### Layer 4: Validation Infrastructure
- Build `cross_validate.py` — compare NSF vs VGM vs NES-MDB
- Build `nsf_trust_scorer.py` — automated trust assessment per game
- Gate: must produce actionable trust scores for at least 10 games

### ANTIRIPPER
- Wire pipeline hooks (`ANTIRIPPER/scripts/pipeline_hooks_v2.py`)
- Integrate into batch scripts for auto-evidence registration
- Assign driver families to games via survey data

### Downloads
- Nintendo, Hudson, Namco, Taito publishers still untouched on joshw.info (~400+ games)

## Key Files to Read
- `CLAUDE.md` — project overview (Hiro plan status updated)
- `extraction/frame_ir.py` — NEW chip-agnostic core (Layer 2 output)
- `docs/MULTI_CHIP_SCHEMA.md` — expansion schema (Layer 1 output)
- `tests/test_frame_ir_expansion.py` — 24 expansion tests
- `scripts/nsf_to_reaper.py` — emulator with expansion capture (Layer 3)
- `scripts/batch_nsf_all.py` — improved batch extractor
